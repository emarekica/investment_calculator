import { render, screen } from "testing-library/react";
import Results from "./Results";

describe("Results component", () => {
  test("Description", () => {
    render(<Results />);

    const resultsElement =
  });


// renders a table
// renders correct column headers
// renders one row per result year

// calculates total interest correctly for first year
// calculates total interest correctly for later years
// calculates invested capital correctly

// Assert that formatted strings are rendered
});

// Arrange
// Act
// Assert

    // const headerElement = screen.getByText("React investment calculator", { exact: false });
    // expect(headerElement).toBeInTheDocument();
